// Package core 承载 ITH5 的纯逻辑：权限解析、内容校验和、同步计划与路径安全。
//
// 约束（技术方案 §4）：
//   - 零第三方依赖。这些逻辑“错了会出安全问题”，必须可被完整审计。
//   - 不访问网络、数据库或文件系统。所有外部状态由调用方以参数传入。
//   - server 与 CLI 共用同一份实现，因此不存在跨端行为漂移。
//
// 分支覆盖要求接近 100%（技术方案 §19）。
package core
